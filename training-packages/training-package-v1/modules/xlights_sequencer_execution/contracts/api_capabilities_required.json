{
  "requiredCapabilities": [
    "health",
    "sequence.getRevision",
    "sequence.open",
    "sequence.save",
    "sequence.renderCurrent",
    "sequencing.applyBatchPlan",
    "jobs.get",
    "layout.getModels",
    "layout.getScene",
    "timing.getTracks",
    "timing.getMarks",
    "effects.getWindow"
  ],
  "requiredApiVersion": "xlightsdesigner-owned-api",
  "applyModel": "owned-batch-plan-only",
  "sourceSpec": "specs/xlightsdesigner-local-completion-roadmap-2026-04-23.md",
  "notes": [
    "Apply execution must use /xlightsdesigner/api/sequencing/apply-batch-plan with the expected revision token.",
    "Unsupported command graphs must fail closed instead of falling back to transaction or legacy automation endpoints.",
    "Post-apply verification reads back revision state, timing marks, effects windows, and render evidence when available.",
    "Sequence-level rendering controls are planner context only until the owned API exposes explicit sequence settings mutation."
  ]
}
