{
  "requiredCapabilities": [
    "system.validateCommands",
    "sequence.getRevision",
    "sequence.setSettings",
    "transactions.begin",
    "transactions.commit",
    "transactions.rollback",
    "timing.getTracks",
    "timing.getMarks",
    "effects.list"
  ],
  "requiredApiVersion": 2,
  "applyModel": "transactions-only",
  "sourceSpec": "specs/sequence-agent/xlights-sequencer-control-api-surface-contract.md",
  "notes": [
    "Apply execution must use transactions.begin/stage/commit with expectedRevision.",
    "system.executePlan is deprecated for sequence_agent runtime flow.",
    "Post-apply verification reads back timing marks and effects.list results.",
    "sequence.setSettings is required for sequence-level rendering controls such as supportsModelBlending when broad group coverage is refined by more specific targets."
  ]
}
